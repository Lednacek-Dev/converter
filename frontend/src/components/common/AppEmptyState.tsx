import styled from "styled-components";

export const AppEmptyState = styled.div`
  padding: 3rem 2rem;
  text-align: center;
  color: ${({ theme }) => theme.textSecondary};
  font-size: 0.875rem;
`;
